env: "local" # local, dev, prod
ws_server:
  address: ":5051"
http_server:
  address: ":80"
  timeout: 4s
  idle_timeout: 60s
mysql:
  max_open_connections: 10
  max_idle_connections: 10
  max_connection_lifetime: 5m
env_state:
  local: "local"
  dev: "dev"
  prod: "prod"